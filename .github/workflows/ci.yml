name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:
    inputs:
      extended:
        description: Run the Windows and Node 20 compatibility matrix.
        required: false
        default: false
        type: boolean

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Ubuntu / Node 22
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - name: Install Linux system test dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg fonts-noto-cjk
      - name: Install Node dependencies
        run: npm ci
      - name: Install pronunciation test dependencies
        run: python -m pip install -r requirements-test.txt
      - name: Install Playwright Chromium on Linux
        if: runner.os == 'Linux'
        run: npx playwright install --with-deps chromium
      - name: Run complete CI test suite
        run: npm run test:ci
        env:
          SCENE_GEN_PROFILE: ci-offline
      - name: Upload golden screenshots
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: html-template-golden-${{ runner.os }}-node-22
          path: test-results/golden
          if-no-files-found: ignore

  compatibility:
    if: github.event_name == 'workflow_dispatch' && inputs.extended
    name: ${{ matrix.os }} / Node ${{ matrix.node }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        node: [20, 22]
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: ${{ matrix.node }}
          cache: npm
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - name: Install Linux system test dependencies
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y ffmpeg fonts-noto-cjk
      - name: Install Windows system test dependencies
        if: runner.os == 'Windows'
        shell: pwsh
        run: choco install ffmpeg -y --no-progress
      - name: Install Node dependencies
        run: npm ci
      - name: Install pronunciation test dependencies
        run: python -m pip install -r requirements-test.txt
      - name: Install Playwright Chromium on Linux
        if: runner.os == 'Linux'
        run: npx playwright install --with-deps chromium
      - name: Install Playwright Chromium on Windows
        if: runner.os == 'Windows'
        run: npx playwright install chromium
      - name: Run complete CI test suite
        run: npm run test:ci
        env:
          SCENE_GEN_PROFILE: ci-offline
